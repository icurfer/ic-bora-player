# CHANGELOG

- **0.22.0** (2026-09-20) — **선택과 재생헤드를 갈랐다** + **지운 구간은 검은 화면으로 지나간다**.
  필름스트립 본체를 클릭하면 **선택만** 되고 재생은 이어진다 — 예전엔 구간을 고르려고
  누를 때마다 재생이 그리로 튀었다. 재생헤드(빨간 선)는 **눈금 띠**에서 옮긴다(배경을 깔아
  누를 자리를 드러냈다). 재생 중 지운 구간에 들어가면 영상 위에 검은 판이 덮이고
  "삭제된 구간"이 뜬다 — 건너뛰지 않고 그대로 지나가므로 무엇이 빠졌는지 보면서 확인된다.
  (미리보기 버튼은 그대로 건너뛴다 — 그쪽은 결과물 확인용이다.)
- **0.21.0** (2026-09-19) — **재생 라인이 부드럽게 따라간다** + **지운 구간은 검게**.
  재생헤드를 창의 250ms 폴링 대신 **프레임 클록**(`add_tick_callback`)에 맞췄다 — 끊기지
  않고 주사율대로 흐른다. 지운 구간은 반투명이라 필름스트립이 비쳐 헷갈렸는데,
  편집기들처럼 완전히 검게 두고 "삭제됨"을 적는다.
  그 과정에서 두 버그: `Player.paused` 에 setter 가 없어 미리보기를 켜면 예외가 났고
  (읽기만 되던 비대칭을 없앴다), 프레임마다 다시 그리자 썸네일 요청의 120ms 지연이
  매번 밀려 **재생 중에는 한 장도 안 뽑혔다** — 뷰가 바뀔 때만 요청하도록 고쳤다.
- **0.20.2** (2026-09-19) — **필름스트립이 안 보이던 버그 수정**. 썸네일은 제대로 뽑혀
  캐시에 쌓이는데 화면은 빈 띠였다 — **요청 격자와 조회 격자가 어긋났다.** 요청은 시간을
  등분하고(630.9초 간격) 그리기는 픽셀 폭으로 걸어(107px = 632.9초) 캐시 키가 631 인데
  633 을 찾았다. 정확히 맞추려는 설계를 버리고 **가장 가까운 썸네일**을 쓰도록 바꿨다
  (타일 하나 범위 안에서만). 영상 끝을 넘겨 요청하던 것(코드 234)도 접었다.
  필름스트립 채움/빈칸을 debug 로그에 남긴다 — 다음에 같은 증상이 오면 바로 갈린다.
- **0.20.1** (2026-09-19) — **메뉴가 안 열리던 버그 수정**. 항목을 하나씩 더하다 메뉴 높이가
  767px 이 되어 창(560px)을 넘자 **팝오버가 아예 뜨지 않았다** — 하단 메뉴 버튼도, 우클릭
  메뉴도. 스크롤로 감싸 길이에 상관없이 열리게 했다. 하단 버튼은 `direction`·`position` 이
  기본값(아래)이라 화면 밖으로 열리던 것도 위로 바로잡았다. 우클릭 메뉴는 클릭 지점을 화면
  안으로 당기고(오른쪽 끝에서 절반이 잘렸다) 아래쪽에서는 위로 뒤집는다.
  검증: 시나리오 10 에 M1·M2 추가 — 네 귀퉁이 전부 통과.
- **0.20.0** (2026-09-19) — **하단 타임라인 컷 편집**(`clip/timeline.py`, `clip/thumbs.py`).
  v0.4 의 클립 목록으로는 컷 편집이 되지 않는다는 지적을 받아 다시 만들었다 — 기획서 v0.4 가
  "타임라인 편집기를 만들지 않는다"고 그은 선이 틀렸다. `Ctrl+E` 로 펴고 `S` 자르기 ·
  `Delete` 지우기/되살리기 · 경계 드래그 트림 · `Ctrl+Z` · 미리보기.
  **기본이 담기(keep)에서 잘라내기(cut)로 뒤집혔다** — 전체가 놓인 채로 시작해 지워서 줄인다.
  필름스트립은 보이는 구간만 게으르게 뽑는다(전체 훑기는 291.8초, 개별 seek 는 26초 — 실측 §5).
  **deb 의존에 `python3-gi-cairo` 추가** — 없으면 타임라인이 통째로 안 그려진다.
- **0.19.0** (2026-09-19) — **Space 가 먹히지 않던 버그 수정** + **로그 설정 UI**.
  키 컨트롤러가 기본 `BUBBLE` 이라 **포커스된 버튼이 Space 를 먼저 삼켰다** — 시작 포커스가
  파일 열기 버튼이라 Space 가 파일 탐색기를 열었다. `CAPTURE` 로 바꿔 창이 먼저 받게 하고,
  그 대가로 메모·입력칸에 포커스가 있으면 비켜 주는 예외(`_typing()`)를 넣었다.
  로그 등급(조용히·보통·자세히)을 메뉴에서 고르고 파일로도 남길 수 있다 — **즉시 적용**되고
  설정에 남는다. 버그는 재현되는 순간에 등급을 올려야 잡히지 재시작하면 증상이 사라진다.
  검증: 시나리오 10 (K1~K4 · L1~L3) 전부 통과.
- **0.18.0** (2026-09-19) — **클립 — 재생 중 잘라내기·이어붙이기**(`src/bora/clip/`). 보는 중에 `K` 로
  구간을 담고 `Shift+K` 목록에서 순서·제목을 고쳐 내보낸다. 자르기는 **빠르게**(무손실, 장면 경계로
  밀림)와 **정확히**(재인코딩) 두 모드. 전부 ffmpeg 에 위임하고 진행률·취소를 붙였다.
  실측에서 **코덱이 다른 조각을 잇으면 ffmpeg 이 오류 없이 깨진 파일을 만드는 것**을 확인해
  (research 9 §3-E) 내보내기 전에 형식을 대조하는 방어선을 넣었다.
  버그: `p` 키가 재생 토글과 핀 추가에 **중복 등록**돼 재생 토글이 죽어 있었다 — 죽은 항목을 지우고
  README 표를 실제 동작과 맞췄다. deb 의존에 `ffmpeg` 추가.
- **0.17.0** (2026-09-16) — **deb 패키징**(`scripts/build-deb.sh`). 56 KB, 의존성 4개
  (`python3-gi`, `gir1.2-gtk-4.0`, `gir1.2-adw-1`, `python3-mpv`). 실제 설치·재생 확인.
  선택 기능(AI·텍스트 추출)은 deb 에서 빼고 설치 후 각자 넣는다.
  배포 방식 비교는 research 8 — **컨테이너는 서버용이라 쓰지 않는다**.
- **0.16.0** (2026-09-14) — **AI 질의**(Claude API). 메모의 줄에 커서를 두고 `Ctrl+Enter` 하면 그 줄을
  질문으로 보내고, 답이 **그 아래 인용 블록으로 스트리밍**된다. 맥락은 자막 전체(캐시) + 지금 대목 +
  메모 부근. 답변마다 비용을 표시한다. **자동으로 부르지 않는다.**
  **앱은 API 키를 저장하지 않는다** — `ANTHROPIC_API_KEY` 나 `ant` 프로필을 SDK 가 읽는다.
  선택 설치(`bash scripts/install-ai.sh`). ⚠ 실제 호출은 키가 있어야 시험된다.
- **0.15.0** (2026-09-14) — **음성 텍스트 추출**(faster-whisper). 자막 없는 영상을 열면 제안하고,
  별도 프로세스로 돌려 진행률을 보여 주며 취소된다. 결과는 영상 옆 `.srt` 로 저장되고 바로 붙는다.
  **선택 설치**(`bash scripts/install-stt.sh`, 436MB) — 없어도 플레이어는 그대로 동작한다.
  실측: 60초 오디오가 `tiny` 14초 · `small` 54초.
  **핀에 제목 입력**: 핀을 꽂으면 메모가 열리고 그 줄 끝에 커서가 와 바로 제목을 칠 수 있다.
- **0.14.0** (2026-09-14) — **핀을 꽂으면 메모에도 남는다**. 구간 핀은 `## [00:15:00] ~ [00:19:00] 라벨` 로
  들어가고, **그 줄을 누르면 그 구간 반복이 바로 걸린다**. 시점 핀은 `## [00:55:00] `.
  패널이 닫혀 있어도 남긴다 — 핀과 메모가 따로 놀면 되돌아볼 때 둘을 맞춰 봐야 한다.
- **0.13.0** (2026-09-14) — **자막 위치 조절**(`R` 위 / `Shift+R` 아래, 메뉴 슬라이더). 강의 영상은 하단에
  슬라이드 글자가 있어 자막이 겹치는 일이 잦다. 설정에 저장되어 다음에도 유지된다.
  이로써 국내 플레이어 비교(research 7)에서 꼽은 1~3순위 격차를 모두 채웠다 — 남은 것은
  플레이리스트와 밝기뿐이다.
- **0.12.0** (2026-09-14) — **구간 반복(A-B)**과 **핀**. 버튼 하나로 A→B→해제 순환(`A` 키), 곰·KMP 관례인
  `F5`/`F6` 도 지원. mpv 내장 `ab-loop` 를 쓴다. **핀은 시점과 구간을 한 모델로** — 구간이 잡혀 있을 때
  꽂으면 구간 핀이 되고, 고르면 그 구간 반복이 바로 걸린다(`P`). 파일별로 저장된다.
- **0.11.1** (2026-09-14) — 국내 플레이어 3종(곰·KMP·팟)과 기능 격차 조사(research 7).
  재생 기본기는 거의 따라잡았고(빠진 것: **구간 반복**·자막 위치·플레이리스트), **자막은 앞섰으며**
  (셋 다 싱크를 저장하지 못한다), 학습 기능은 비교 대상이 없다. 다음 1순위는 **구간 반복(A-B)**.
- **0.11.0** (2026-09-14) — **마크다운 라이브 프리뷰**: `#`·`**` 같은 마크업을 감추고 스타일만 보여 준다.
  커서가 간 줄은 원본을 드러내 고칠 수 있다(Obsidian 방식). **UI 재편**: 헤더바 버튼 6개를
  하단 메뉴 하나로 모았다 — 전체화면에서도 손이 닿는다.
  **버그**: 감춘 마크업이 저장 파일에서 사라지던 것을 고쳤다(`get_text` 의 `include_hidden_chars`).
- **0.10.0** (2026-09-14) — **학습 메모 패널** (v0.3 §9 의 1단계). 영상 옆 `<이름>.md` 에 마크다운으로
  받아 적는다. `Ctrl+T` 로 현재 시각을 제목으로 넣고, 본문의 `[HH:MM:SS]` 를 누르면 그 시점으로 간다.
  입력이 멈추면 2초 뒤 자동 저장(원자적). 밖에서 고친 파일은 말없이 덮지 않는다.
  수용 테스트 N1~N5 통과.
- **0.9.1** (2026-09-14) — **v0.3 기획서** 작성(`docs/spec/plan-v0.3.0.md`). 제품 정의를 "국내 자막 플레이어"에서
  **"로컬 강의로 공부하는 사람을 위한 학습 도구"**로 다시 잡았다. 텍스트 추출·학습 메모·AI 질의 설계와
  하지 않을 것의 경계. §8 실측: PEP 668 로 시스템 파이썬에 `anthropic` 설치 불가 →
  `venv --system-site-packages` 로 해결(GTK 4.22 + anthropic 1.5.0 동시 사용 확인).
- **0.9.0** (2026-09-14) — **기본 영상 플레이어 설정**을 앱 안에 넣었다(헤더바 ☰ 스위치).
  켜기 전 값을 기억해 두어 끄면 이전 프로그램으로 정확히 되돌린다.
  `.desktop` 을 등록하면 데스크톱이 기본 프로그램을 가져가는 경우가 있어(실측) 사용자가
  직접 제어할 수 있어야 했다.
- **0.8.1** (2026-09-14) — 앱 아이콘과 `.desktop` 추가. `scripts/install-desktop.sh` 로 앱 목록에 등록하면
  터미널 없이 실행되고 영상 파일 "연결 프로그램"에도 뜬다(기본 프로그램은 바꾸지 않는다).
- **0.8.0** (2026-09-14) — v0.2 마무리. **자막 글꼴·색 선택 UI**, **재생 속도 음정 유지**,
  **자막 없는 스크린샷**(`Shift+C`) 추가 — "다 된 건가?" 물음에 코드를 훑어 빠진 것을 찾아 채웠다.
  수용 테스트 R1·S1~S3 검증 추가(시나리오 04 가 15항목). 작업내역 `docs/done/done-v0.7.0-2026-09-14.md`.
  요구 #9~#14 전부 완료. **22.04·24.04 는 후속으로 미룬다.**
- **0.7.0** (2026-09-14) — **자막 에디터**. 보면서 고친다: 일시정지한 시각을 그 큐의 시작·끝으로 박고,
  이 줄부터 뒤로 밀고, 저장하면 `.bak` 을 남긴 뒤 바로 화면에 다시 물린다.
  SAMI 는 `.srt` 로 저장하고 원본은 그대로 둔다. 수용 테스트 E1~E4 통과.
  **버그**: python-mpv 의 `seek(reference='absolute')` 가 0 근처로 되감긴다 — 명령을 직접 보내도록 수정.
  이 때문에 특정 시각으로 이동하는 기능이 전부 어긋나 있었다.
- **0.6.0** (2026-09-14) — v0.2 기획 착수. 기획서 `docs/spec/plan-v0.2.0.md` 작성(조사: 재생 중 큐 타이밍을
  찍어 저장하는 플레이어는 사실상 없다 — 편집기는 감상용이 아니고 플레이어는 저장을 못 한다).
  **설정 기반**(`state.py`) + **이어보기**·**최근 파일**·**재생 속도**·**화면 비율·확대**·**스크린샷**·
  **자막 크기** 구현. 검증 시나리오 04 추가(10항목).
- **0.5.0** (2026-09-14) — **오디오 트랙 선택** 추가(코멘터리·더빙 등). 트랙 이름을 사람이 읽을 수 있게
  바꿨다(`src/bora/tracks.py`) — 언어를 한글로, 이미지 자막·외부 자막·기본 트랙을 표시.
  **우클릭 메뉴가 좌상단에 뜨던 버그 수정** — `Gdk.Rectangle(x=…)` 생성자 키워드가 조용히 무시돼
  (0,0)이 들어갔다. 팝오버도 창이 아니라 영상 위젯에 붙여 좌표계를 맞췄다.
  검증 시나리오가 사용자 창에 흡수되지 않도록 `non_unique` 옵션 추가.
- **0.4.0** (2026-09-14) — UI 다듬기.
  **우클릭 메뉴** 추가 — 재생/정지·탐색·자막 싱크·전체화면·열기, 항목마다 단축키 병기.
  전체화면에서 바를 감춘 동안에도 조작할 수 있다.
  **버튼 툴팁에 단축키 표기**. **바가 접히고 펴지는 전환**을 `Gtk.Revealer`(250ms)로 부드럽게.
- **0.3.3** (2026-09-14) — 전체화면 버그 두 건 수정.
  **검은 화면**: `set_auto_render(False)` 라 창 크기가 바뀌어도 다시 그리지 않아, 전체화면을
  오갈 때 검은 화면이 남았다 — GLArea `resize` 에서 다시 그린다.
  **UI 가 안 사라짐**: 전체화면인데 헤더바·컨트롤이 계속 떠 영상을 가렸다 — 감추고,
  마우스를 움직이면 잠깐 보여준 뒤 다시 감춘다(커서도 함께).
  전체화면 처리를 `notify::fullscreened` 구독으로 바꿔, 창 관리자·단축키 등 **어느 경로로 바꿔도**
  UI 가 일관되게 따라간다.
- **0.3.2** (2026-09-14) — **실제 자막 표본에서 P5 발견·수정**: `&nbsp;` 만 있는 큐가 화면에 빈 칸으로
  찍혀 "자막이 안 나온다"로 보였다(실제 영화 자막 2665 큐 중 1332 개). `--sub-filter-regex` 로
  파일을 다시 쓰지 않고 걸러낸다.
  **로그 추가** — `--debug` / `BORA_DEBUG=1` 로 앱과 libmpv 로그를 함께 본다(`BORA_LOG_FILE` 지원).
  **정지 버튼 추가** — 처음으로 되돌리고 멈춘다(파일은 열어 둠, 단축키 `s`).
- **0.3.1** (2026-09-14) — scope §4 7단계 완료: 드래그 앤 드롭(창 전체), 전체화면, 단축키
  (space·f·F11·Esc·좌우·상하·대괄호·o). 검증 시나리오 03(UI 동작 11항목) 추가.
  작업내역 `docs/done/done-v0.3.0-2026-09-14.md`.
  시나리오 01 의 판정에서 초당 렌더 기준을 뺐다 — 창이 가려지면 정상인데도 실패로 잡힌다.
- **0.3.0** (2026-09-14) — **기획서 §6 수용 테스트 T1~T4 통과.** scope §4 4~6단계 완료.
  `subtitle/loader.py`(무엇을 할지 정하는 층) + Player 자막 API + 자막 UI(트랙 선택·싱크 조절·상태 표시).
  CP949 SAMI 를 영상 옆에 두면 설정 없이 한글 자막이 나오고, 한·영 통합 SAMI 는 트랙 2개로 갈려
  한국어가 기본 선택된다. 검증: `verify-app/scenarios/02-subtitle-acceptance.py`.
- **0.2.1** (2026-09-13) — scope §4 2·3단계: 자막 판정(`subtitle/detect.py`)과 SAMI 분리(`subtitle/sami.py`)를
  프로토타입에서 승격, 테스트 26개로 고정.
  **Gate F 가 죽어 있던 것을 발견해 되살렸다** — grep 의 바이트 범위 패턴이 아무것도 매칭하지 않아
  한자 검사가 조용히 무력화돼 있었다. 검사를 `scripts/check_taboo.py` 로 옮기고
  `scripts/selftest_taboo.sh`(미탐·오탐 양쪽 검증)를 추가했다.
- **0.2.0** (2026-09-13) — **첫 코드.** scope §4 1단계 — 창이 뜨고 영상이 재생된다.
  `src/bora/{__main__,app,window,player,glarea,util/gl}.py`. 스파이크 코드를 승격했고 PyOpenGL 을 쓰지 않는다.
  **버그 수정**: libmpv 가 `LC_NUMERIC` 이 C 가 아니면 초기화를 거부한다 — `Player.__init__` 에서 보정.
  검증: `verify-app/scenarios/01-playback-renders.py` — Wayland 네이티브, 초당 23회 렌더, hwdec 활성.
- **0.1.4** (2026-09-13) — **26.04 우선 개발로 결정**, 22.04·24.04 대응과 배포 방식(8-1) 선택은 연기.
  `scope-v0.1.0.md`(파일·함수 단위 구현 범위) + `deferred/backlog-v0.1.0.md` 작성.
  UI 위젯은 libadwaita 1.1 범위를 기본으로 쓰는 규율을 채택 — 26.04 에서 비용이 0 이고 나중 이식 비용을 줄인다.
- **0.1.3** (2026-09-13) — A 안의 유일한 장벽을 실측. jammy 의 libadwaita 1.1 / GTK 4.6 에
  **`Gtk.GLArea` 가 있고**, 없는 위젯(`ToolbarView`·`SpinRow`·`MessageDialog`·`FileDialog`)은 전부 대체재가 있다.
  → A 안은 실현 가능. 8-1 결정 재료가 모두 모였다.
- **0.1.2** (2026-09-13) — 기획서 §8-2 확정, 8-1 재료 확보. **Flatpak 포털만으로는 동명 자막 자동 로드가
  구조적으로 불가능**함을 실측(고른 파일 하나만 노출) — B 안은 `--filesystem=host:ro` 가 필수다.
  Flatpak 하드웨어 디코딩은 호스트와 동일함을 확인. §4 배포 방식 판단을 "트레이드오프 하나"로 재정리.
- **0.1.1** (2026-09-13) — 기획서 §8 검증 6/8 처리. libmpv 경로 실측으로 **1차 진단 정정**(P1 은 mpv 에서
  나타나지 않고, 한글이 안 보이는 원인은 P4). 인코딩 판정 파이프라인 확정(11/11), GTK4 렌더 스파이크 성공,
  22.04 호환성 확인. research 문서 2편 + 검증 스크립트 8개 추가.
  라이선스 종속 구조 확정(§9) — 8-8 은 8-1 에 종속된다.
  **`version` 관리 규칙 도입**: 기획 단계에도 `docs/` 산출물이 바뀌면 patch bump (Gate A 가 강제).
- **0.1.0** (2026-09-13) — 기획 단계 시작. 코드 없음.
  기획서 `docs/spec/plan-v0.1.0.md` 작성, 자막 인코딩·SAMI 실측 기록 추가.
  ic-praxis 규율 체계 적용: `CLAUDE.md` 헌법, 커밋 게이트(A~F), 공유 메모리, `verify-app` 스킬.
