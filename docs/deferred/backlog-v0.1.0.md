# deferred/backlog-v0.1.0 — v0.1.0 MVP 밖으로 밀어낸 항목

기준: [`../spec/plan-v0.1.0.md`](../spec/plan-v0.1.0.md) · 작성 2026-09-13

MVP 는 **"국내 자막이 26.04 에서 그냥 나오는 플레이어"** 하나에 집중한다.
아래는 **버리는 것이 아니라 순서를 미룬 것**이다. 각 항목에 "언제 다시 볼지"를 적는다.

## 1. 플랫폼·배포

| 항목 | 왜 미루나 | 언제 다시 보나 |
|---|---|---|
| **Ubuntu 22.04 · 24.04 지원** (요구 #3) | 22.04 VM 검증에 드는 시간을 지금은 구현에 쓴다. 개발 머신이 26.04 다 | v0.1 이 26.04 에서 동작한 뒤. `scripts/spike_gtk4.py` 를 22.04 VM 에서 돌리는 것이 첫 단계(기획서 8-5) |
| **배포 방식 결정 (A 시스템 패키지 / B Flatpak)** | 26.04 우선 개발이면 이 선택이 구현을 막지 않는다 | 22.04 대응 시작 시. 재료는 기획서 §4 에 정리돼 있다 |
| **Flatpak 매니페스트 · Flathub 배포** | B 안을 고를 때만 필요하다. `--filesystem=host:ro` 가 필수라는 대가도 확인됐다(research 4) | 8-1 을 B 로 정하면 |
| ~~deb / pipx 패키징~~ | ✅ **v0.17.0 에서 deb 를 만들었다**(`scripts/build-deb.sh`, 56 KB). 설치·재생 확인 — research 8 | 완료 |
| **Windows / macOS** | 기획서 §3-3 의 비목표 | 없음 |

## 2. 자막 기능

| 항목 | 왜 미루나 | 언제 다시 보나 |
|---|---|---|
| **자막 사이트 검색·자동 다운로드** | 기획서 §3-3 비목표. 외부 서비스 의존이 생긴다 | v0.2 이후 |
| **자막 인코딩 수동 지정 UI** | v0.1 은 자동 판정 + "추정" 배지까지. 판정이 틀렸을 때의 교정 수단 | 실제 자막 표본에서 오판이 관찰되면 |
| **SAMI 외 다국어 컨테이너**(다중 클래스 3개 이상, `lang:` 없는 변종) | 표본이 없다. 지금 설계하면 추측이 된다 | 실제 자막을 모은 뒤(아래 참고) |
| **자막 싱크 자동 보정** | 사람이 ±0.1초 버튼으로 맞추는 것으로 충분하다 | v0.2 이후 |
| **`.ass` 스타일 보존 변환** | SAMI→SRT 로 충분하다. ASS 스타일까지 만들면 범위가 커진다 | 사용자 요구가 생기면 |

## 3. 플레이어 기능

| 항목 | 왜 미루나 | 언제 다시 보나 |
|---|---|---|
| 플레이리스트 · 라이브러리(미디어 센터) | 기획서 §3-3 비목표 | v0.2 이후 |
| 스트리밍 URL 재생 · yt-dlp 연동 | 로컬 파일 문제부터 푼다 | v0.2 이후 |
| 오디오 트랙 선택 · 화면 비율 · 필터 | libmpv 가 다 하지만 UI 를 붙이는 비용이 있다 | v0.1 이 동작한 뒤 하나씩 |
| 단축키 사용자 지정 · 설정 창 | 기본 단축키 고정으로 시작 | v0.2 이후 |
| 로고 · 아이콘(보라색 키 컬러) | 기능이 먼저다 | 공개 직전 |

## 4. 품질·검증

| 항목 | 왜 미루나 | 언제 다시 보나 |
|---|---|---|
| **실제 자막 표본 수집(수십 개)** | 지금 표본은 합성 11개다. 임계값 0.7 의 근거가 약하다 | **v0.1 구현과 병행한다 — 미루되 잊지 않는다.** `tests/fixtures/` |
| 자동화된 GUI 테스트 | Wayland 자동화 제약이 크다 | 필요해지면 |
| 다국어 UI(번역) | 한국어 단일로 시작 | 공개 후 요구가 생기면 |
